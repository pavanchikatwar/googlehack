/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['placehold.co'], // Add the domain here
    },
    // Any other configurations you may have
  };
  
  module.exports = nextConfig;
  